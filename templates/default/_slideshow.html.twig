{% set menuOpen = true %}
<!-- .block-slideshow -->
<div class="block-slideshow block-slideshow--layout--with-departments block">
    <div class="container">
        <div class="row">
            <div class="col-lg-3 d-none d-lg-block"></div>
            <div class="col-12 col-lg-9">
                <div class="block-slideshow__body">
                    <div class="owl-carousel">
                        {% for item in carouselItems %}
                            <a class="block-slideshow__slide"
                               href="{{ item.link }}">
                                <div class="block-slideshow__slide-image block-slideshow__slide-image--desktop"
                                     style="background-size: cover; background-image: url('{{ item.idImage|image }}')"></div>
                                <div class="block-slideshow__slide-image block-slideshow__slide-image--mobile"
                                     style="background-size: cover; background-image: url('{{ item.idImage|image }}')"></div>
                                <div class="block-slideshow__slide-content">
                                    <div class="block-slideshow__slide-title">{{ item.title }}</div>
                                    {# <div class="block-slideshow__slide-text">{{ product.shortDescription }}</div> #}
                                    <div class="block-slideshow__slide-button">
                                        <span class="btn btn-primary btn-lg">Go to link</span>
                                    </div>
                                </div>
                            </a>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- .block-slideshow / end -->